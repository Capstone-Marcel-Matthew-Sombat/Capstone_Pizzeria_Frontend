import React, { useState } from 'react';

const Cart = ({cart}) => {
    console.log(cart)

    return (
        <div>
            <h1>this is cart</h1>
            {/* items in your cart */}
        </div>
    );
};

export default Cart;
